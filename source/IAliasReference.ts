/**
 * Represents a reference of the alias that is been modified.
 */
export interface IAliasReference {
    /**
     * Returns the alias.
     * @returns {string} The context alias.
     */
    getAlias():string;
}